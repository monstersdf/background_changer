let  colorButtons = document.getElementsByClassName('bg-changer')
// alert(colorButtons);

let btns = document.getElementById('ki')
console.log(btns);

function colorChanger(color){
    // document.body.style.backgroundColor = color;
   btns.style.backgroundColor = color;
    // alert();
}